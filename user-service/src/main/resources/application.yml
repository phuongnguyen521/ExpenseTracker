server:
  port: 0 # Set the port for your Spring Boot application

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost

spring:
  application:
    name: user-service
  profiles:
    active:
  datasource:
    url: jdbc:mysql://sql12.freesqldatabase.com:3306/sql12792329 # URL for MySQL host
    driver-class-name: com.mysql.cj.jdbc.Driver # The JDBC driver for MySQL
    username: sql12792329
    password: FwxZQLeQm5

  jpa:
    hibernate:
      ddl-auto: update # Automatically creates or updates the database schema based on entity classes
    show-sql: true #Enables logging of SQL queries generated by JPA
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

# Logging configuration
logging:
  level:
    root: INFO
    com.phuong: DEBUG
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/user-service.log
    max-size: 10MB
    max-history: 30

# Management and Monitoring
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,loggers
  endpoint:
    health:
      show-details: always
  health:
    show-components: always
  metrics:
    export:
      prometheus:
        enabled: true
